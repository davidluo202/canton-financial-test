# ============================================
# SMTP Email Service Configuration Template
# SMTP 邮件服务配置模板
# ============================================

# SMTP Server Settings / SMTP服务器设置
# ----------------------------------------
# For SendGrid: smtp.sendgrid.net
# For AWS SES: email-smtp.<region>.amazonaws.com (e.g., email-smtp.us-east-1.amazonaws.com)
# For Gmail: smtp.gmail.com (testing only, not recommended for production)
SMTP_HOST=smtp.sendgrid.net

# SMTP Port / SMTP端口
# ----------------------------------------
# 587 - TLS (recommended for most providers)
# 465 - SSL (alternative, supported by most providers)
# 25  - Plain (not recommended, often blocked)
SMTP_PORT=587

# SMTP Authentication / SMTP认证
# ----------------------------------------
# For SendGrid: use "apikey" as username, your API key as password
# For AWS SES: use your SES SMTP username and password
# For Gmail: use your email address and app-specific password
SMTP_USER=apikey
SMTP_PASS=your-smtp-password-or-api-key-here

# Optional: Custom Sender Email / 可选：自定义发件人邮箱
# ----------------------------------------
# If not set, SMTP_USER will be used as the sender
# 如果未设置，将使用SMTP_USER作为发件人
# SMTP_FROM=noreply@cmfinancial.com

# Optional: Custom Schedule / 可选：自定义计划
# ----------------------------------------
# Cron expression for daily report schedule
# Default: 0 0 9 * * * (9:00 AM Hong Kong Time)
# Format: second minute hour day month weekday
# DAILY_REPORT_CRON=0 0 9 * * *

# ============================================
# Quick Setup Examples / 快速设置示例
# ============================================

# Example 1: SendGrid
# -------------------
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Example 2: AWS SES (US East 1)
# -------------------------------
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USER=AKIAIOSFODNN7EXAMPLE
# SMTP_PASS=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

# Example 3: Gmail (Testing Only)
# --------------------------------
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-16-char-app-password

# ============================================
# Testing / 测试
# ============================================
# After configuration, test with:
# 配置后，使用以下方式测试：
#
# 1. Verify configuration:
#    await trpc.email.verifyConfig.query()
#
# 2. Send test report:
#    await trpc.email.triggerDailyReport.mutate()
#
# 3. Check status:
#    await trpc.email.getStatus.query()
